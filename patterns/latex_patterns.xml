<!-- $Id$-->
<patterns>
 <class name="latex" pattern="/\w+\.(tex|sty)$" options="wrap">

 <!-- highlight styles -->
 <style format="2" name="Comment" color="#ff8c00" />
 <style format="0" name="Braces" color="#0000ff" />
 <style format="0" name="Keyword" color="#ff0000" /> 
 <style format="0" name="Attribute" color="#0000aa" /> 
 <style format="0" name="Math" color="#0000ff" />
<!-- comments -->
 <range_pattern style="Comment" name="Comment">
  <begin>%</begin>
  <end>$</end>
 </range_pattern>

<!-- keyword attribute range -->
 <range_pattern style="Attribute" name="Attribute" options="span">
  <begin>\\((\w+)|[^\s\d])(\[(\s|\d|\w)*\])?\{</begin>
  <end>[^\\]\}</end>
 </range_pattern>

<!-- math -->
 <range_pattern style="Math" name="Math" options="span">
 <begin>\$</begin>
 <end>\$</end>
 </range_pattern>

<!-- parenthesis -->
 <keyword_pattern style="Braces" name="Braces" >
  <keyword>((\{|\[|\]|\})*(\{|\[|\]|\})(\{|\[|\]|\})*)</keyword>
 </keyword_pattern>

<!-- keyword (any string or single non space/digit character following a backslash)-->
 <keyword_pattern style="Keyword" name="Keyword" >
  <keyword>\\((\w+\b)|[^\s\d])</keyword>
 </keyword_pattern>

<!-- keyword (any string or single non space/digit character following a backslash)-->
 <keyword_pattern style="Keyword" name="Keyword2" parent="Attribute" >
  <keyword>\\((\w+\b)|[^\s\d])</keyword>
 </keyword_pattern>

 <!-- parenthesis -->
 <parenthesis begin="(" end=")" />
 <parenthesis begin="[" end="]" regexp="\[|\]" /> 
 <parenthesis begin="{" end="}" regexp="\{|\}" /> 

 <!-- text processing macros -->

 <macro name="Add one tab indentation" accelerator="Alt+T">
  <rule>
   <regexp>^</regexp>
   <replacement>XML_TAB</replacement>
  </rule>
 </macro>

 <macro name="Remove one tab indentation" accelerator="Shift+Alt+T">
  <rule>
   <regexp>^\t</regexp>
   <replacement></replacement>
  </rule>
 </macro>

 <macro name="Remove all tab indentations">
  <rule>
   <regexp>^\t+</regexp>
   <replacement></replacement>
  </rule>
 </macro>

 <macro name="Clean end of line" accelerator="Alt+C">
  <rule>
   <regexp>\s+$</regexp>
   <replacement></replacement>
  </rule>
 </macro>

<macro options="separator"/>
 
 <macro name="Add comments">
  <rule>
   <regexp>^</regexp>
   <replacement>%</replacement>
  </rule>
 </macro>

 <macro name="Remove comments">
  <rule>
   <regexp>%</regexp>
   <replacement></replacement>
  </rule>
 </macro>

 </class>
</patterns>
